spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/unio
    username: unio
    password: unio
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 0
